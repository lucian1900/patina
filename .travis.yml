language: python
python:
    - "2.7"
    - "pypy"
    - "3.3"

matrix:
    allow_failures:
        - python: "3.3"

install:
    - pip install pytest
    - pip install -e .

script: PYTHONPATH="$PYTHNOPATH:." py.test

notifications:
    email: false
